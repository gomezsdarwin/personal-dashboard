# Expo SDK pulls in transitive deps whose React/react-dom peer ranges don't all
# agree under npm's strict peer resolution. legacy-peer-deps lets `npm install`
# resolve cleanly (this is the standard Expo-on-npm workaround, not carelessness).
legacy-peer-deps=true
